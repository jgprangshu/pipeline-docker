pipeline{
    environment {
    registry = "jgprangshu/docker-test"
    registryCredential = 'dockercred'
    dockerImage = ''
    }
    agent any
     tools {
        maven "Maven"
    }
    stages{
        stage('Echo'){
            steps{
                echo 'Hello World'
            }
        }
    }
}
